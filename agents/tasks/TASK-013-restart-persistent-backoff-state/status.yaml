task_id: TASK-013
title: Restart-persistent backoff state for live and VOD collectors
status: done
owner: coordinator
next_owner: none
created_at: 2026-04-08
updated_at: 2026-04-08
blocked_by: []
deliverables:
  - persisted backoff state for reviewed live and one-streamer VOD collector paths
  - evidence artifacts proving restart preserves the backoff window
  - handoff notes for later scheduler and product-hardening slices
files_touched:
  - agents/board/tasks.yaml
  - agents/board/decisions.md
  - agents/board/progress.md
  - agents/tasks/TASK-013-restart-persistent-backoff-state/spec.md
  - agents/tasks/TASK-013-restart-persistent-backoff-state/status.yaml
  - agents/tasks/TASK-013-restart-persistent-backoff-state/handoff.md
  - agents/tasks/TASK-013-restart-persistent-backoff-state/review.md
  - agents/tasks/TASK-013-restart-persistent-backoff-state/revision_log.md
  - agents/tasks/TASK-013-restart-persistent-backoff-state/done.md
  - agents/artifacts/TASK-013-restart-persistent-backoff-state/README.md
  - app_live_vod.py
  - agents/artifacts/TASK-013-restart-persistent-backoff-state/restart_backoff_evidence.json
  - agents/artifacts/TASK-013-restart-persistent-backoff-state/collector_backoff_rows_before_restart.json
  - agents/artifacts/TASK-013-restart-persistent-backoff-state/collector_backoff_rows_after_recovery.json
  - agents/artifacts/TASK-013-restart-persistent-backoff-state/collector_runs_after_restart_recovery.json
  - agents/artifacts/TASK-013-restart-persistent-backoff-state/recent_state_after_restart_recovery.json
  - agents/artifacts/TASK-013-restart-persistent-backoff-state/restart_backoff_verification_notes.txt
tests:
  - exercised live and one-streamer VOD timeout failures with FastAPI TestClient against a temp SQLite DB, then simulated restart by clearing memory and re-running `init_db()`
  - verified persisted `collector_backoffs` rows are restored after restart and immediate post-restart attempts return bounded `backoff_active` skips
  - verified live and VOD fetch-call counts stay flat during immediate post-restart skips, proving no premature upstream hit
  - verified later successful recovery clears persisted backoff rows and leaves durable `collector_runs` evidence
artifacts:
  - agents/artifacts/TASK-013-restart-persistent-backoff-state/README.md
  - agents/artifacts/TASK-013-restart-persistent-backoff-state/restart_backoff_evidence.json
  - agents/artifacts/TASK-013-restart-persistent-backoff-state/collector_backoff_rows_before_restart.json
  - agents/artifacts/TASK-013-restart-persistent-backoff-state/collector_backoff_rows_after_recovery.json
  - agents/artifacts/TASK-013-restart-persistent-backoff-state/collector_runs_after_restart_recovery.json
  - agents/artifacts/TASK-013-restart-persistent-backoff-state/recent_state_after_restart_recovery.json
  - agents/artifacts/TASK-013-restart-persistent-backoff-state/restart_backoff_verification_notes.txt
notes:
  - created immediately after TASK-012 closeout to keep the live/VOD stabilization track moving
  - summary_engineer is the first execution owner because the restart-persistence work stays inside app_live_vod.py and current collector state handling
  - this slice should preserve existing timeout classification and visibility behavior while making backoff windows survive restart
  - same-role micro-loop may be used only for tiny follow-up work inside this slice, up to the shared limit of 2 consecutive micro-loops
  - `app_live_vod.py` now persists backoff windows in SQLite `collector_backoffs` rows and reloads them during `init_db()` so reviewed retry windows survive restart
  - `get_active_backoff_message()` and `list_active_backoffs()` now consult persisted backoff rows, not just in-memory state
  - saved evidence proves the remaining retry window is restored after simulated restart and still produces bounded skipped behavior before later successful recovery
  - later scheduler hardening still depends on process-level restart tests under the real scheduler loop rather than the current manual restart simulation
  - coordinator reviewed the saved restart-persistence evidence and determined the slice is ready to route to reviewer
  - same-role micro-loop is no longer the active path because the slice has crossed into a review handoff boundary
  - reviewer confirmed that the saved evidence is sufficient to prove persisted retry-window rows, restored skip behavior after restart, bounded upstream pressure, and later successful recovery for both live and one-streamer VOD paths
  - coordinator closed TASK-013 after reviewer marked the slice ready for acceptance; the next needed role moves back to summary_engineer for scheduler hardening while publisher_engineer, ops, and reviewer return to normal idle state
